---
title: ''
---

This page explains the core concepts behind **versionguard**.

## Entry points

VersionGuard exposes a few distinct entrypoint modules depending on the kind of work you want to do.

- `src/index.ts` is the main public API surface for validation, sync, doctor, and shared exports.
- `src/feedback/index.ts` contains suggestion builders that turn validation failures into actionable guidance.
- `src/fix/index.ts` contains deterministic auto-remediation helpers for versions, changelog entries, and synced files.
- `src/tag/index.ts` contains release-tag helpers for reading, validating, and creating tags safely.

When you consume the package, these map to the main `versionguard` entrypoint and the focused feedback, fix, and tag entrypoint modules generated from the source tree.

> This is a stub page. Edit this file to add your project's conceptual documentation.
>
> Auto-generated sections below (inside FORGE:AUTO markers) update on every build.

{/* FORGE:AUTO-START how-it-works */}

## How It Works

versionguard processes your TypeScript source with a single AST traversal, extracting TSDoc comments and type information to generate documentation.

{/* FORGE:AUTO-END how-it-works */}

{/* FORGE:AUTO-START key-abstractions */}

## Key Abstractions

- **`VersioningType`** — Supported versioning strategies.    0.1.0
- **`CalVerFormat`** — Supported calendar version string layouts.    0.1.0
- **`CalVerConfig`** — Configures CalVer validation rules.    0.1.0
- **`SyncPattern`** — Describes a search-and-replace pattern used during version synchronization.    0.1.0
- **`SyncConfig`** — Configures files and patterns that should stay in sync with the canonical version.    0.1.0
- **`ChangelogConfig`** — Controls changelog validation behavior.    0.1.0
- **`GitHooksConfig`** — Toggles each supported git hook integration.    0.1.0
- **`GitConfig`** — Configures git-related enforcement.    0.1.0
- **`VersioningConfig`** — Configures the active versioning mode.    0.1.0
- **`VersionGuardConfig`** — Top-level configuration consumed by versionguard.    0.1.0
- **`SemVer`** — Parsed semantic version components.    0.1.0
- **`CalVer`** — Parsed calendar version components.    0.1.0
- **`ParsedSemVer`** — Parsed semantic version result wrapper.    0.1.0
- **`ParsedCalVer`** — Parsed calendar version result wrapper.    0.1.0
- **`ParsedVersion`** — Union of supported parsed version payloads.    0.1.0
- **`ValidationError`** — Describes a single validation problem.    0.1.0
- **`ValidationResult`** — Result returned by version parsing and validation helpers.    0.1.0
- **`SyncChange`** — Describes a single in-file version replacement.    0.1.0
- **`SyncResult`** — Reports the result of synchronizing a single file.    0.1.0
- **`VersionMismatch`** — Reports a discovered version mismatch.    0.1.0
- **`FullValidationResult`** — Combined result from a full project validation run.    0.1.0
- **`DoctorReport`** — Reports whether a project is ready to pass VersionGuard checks.     0.1.0
- **`ParsedCalVerFormat`** — Parsed token layout for a supported CalVer format string.    0.1.0
- **`ChangelogValidationResult`** — Describes the outcome of validating a changelog file.    0.1.0
- **`Suggestion`** — Feedback entry point exports for suggestion and guidance helpers.     0.1.0
- **`FeedbackResult`** — Aggregates validation errors with suggested next steps.    0.1.0
- **`PackageJson`** — Minimal shape of a `package.json` document used by VersionGuard.    0.1.0
- **`FixResult`** — Fix entry point exports for auto-remediation helpers.     0.1.0
- **`TagInfo`** — Tag entry point exports for release-tag management helpers.     0.1.0

{/* FORGE:AUTO-END key-abstractions */}
